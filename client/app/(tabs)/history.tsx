import { AppText, Button, Screen } from "@/components/ui";

export default function History() {
  return (
    <Screen className="items-center justify-center">
      <AppText variant="h2">We got history baby 0!</AppText>
      <Button />
    </Screen>
  );
}
